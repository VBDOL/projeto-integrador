# Frontend - Projeto Integrador (FACULDADE GRAN)

Stack: React (create-react-app minimal)

Como rodar localmente:

```bash
cd frontend
npm install
npm start
```

A aplicação espera que o backend rode em http://localhost:3001 (variável REACT_APP_API_URL pode ser usada).
